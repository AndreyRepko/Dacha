<Window x:Class="Dacha.Inspector.Dictionaries.DictionaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dacha.Inspector.Dictionaries"
        mc:Ignorable="d"
        Title="DictionaryWindow" Height="300" Width="300"
        d:DataContext="{d:DesignInstance Type=local:DictionaryViewModelDummy, IsDesignTimeCreatable=True}">
    <StackPanel>
        <DataGrid ItemsSource="{Binding Dictionary}" IsReadOnly="True" ></DataGrid>
        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding AddCommand}">Add</Button>
            <Button Command="{Binding EditCommand}">Edit</Button>
            <Button Command="{Binding RemoveCommand}">Remove</Button>
        </StackPanel>
    </StackPanel>
</Window>
